results_config: {}

ssh_config:
  port: 31222
  code_path: /jobs
  key_path: ~/.ssh/id_foundations_scheduler
  user: job-uploader

worker:
  image: python:3.6
  workingDir: /job/job_source
  command:
    - /bin/bash
    - -c
    - pip install -r requirements.txt && python custom_main.py
