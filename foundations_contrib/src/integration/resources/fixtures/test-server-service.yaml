apiVersion: v1
kind: Service
metadata:
  name: project-model-service
  namespace: ingress-nginx-test
spec:
  selector:
    app: project-model
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
